## Статус на 30.11.2025

- Бот переведен на нового VK-партнера `@EFGR_SherlockSearchbot`: обновлены переменные `VK_BOT_USERNAMES`/`VK_BOT_USERNAME` и создана рабочая Telegram-сессия через `/session_auth`.
- Запуск поставлен в `nohup python run.py` (PID 3804980); бот онлайн, баланс читается, воркеры готовы к поиску.
- Переработан тайминг:
  * межзапросная задержка — 2.0–5.0 с (доступно через `EXTRA_INTER_REQUEST_DELAY_*`);
  * длинная пауза срабатывает каждые 50 запросов и длится 5.2–7.8 с (`GLOBAL_PAUSE_*`).
- Оценка пессимистичного поведения: 4000 запросов при таких задержках займут максимум ~5.7 ч.
