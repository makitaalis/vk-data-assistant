# План доработки проекта VK Data Assistant

## 📋 Общее описание
План поэтапной доработки Telegram бота для поиска контактных данных по ссылкам VK. 
Цель: устранение критических ошибок, улучшение UX и добавление новой функциональности.

---

## 🚨 ЭТАП 1: Критические исправления (1-2 дня)

### 1.1 Исправление методов базы данных
**Проблема:** Отсутствуют методы `get_all_users()` и `get_users_statistics()`, из-за чего не работают админские команды.

**Решение:**
```python
# В database.py добавить:

async def get_all_users(self) -> List[Dict]:
    """Получение списка всех пользователей"""
    async with self.acquire() as conn:
        rows = await conn.fetch("""
            SELECT user_id, username, first_name, last_name, 
                   last_activity, accepted_disclaimer
            FROM users 
            ORDER BY last_activity DESC
        """)
        return [dict(row) for row in rows]

async def get_users_statistics(self) -> Dict[str, int]:
    """Статистика по пользователям"""
    async with self.acquire() as conn:
        result = await conn.fetchrow("""
            SELECT 
                COUNT(*) as total_users,
                COUNT(*) FILTER (WHERE last_activity > NOW() - INTERVAL '7 days') as active_7d,
                COUNT(*) FILTER (WHERE last_activity > NOW() - INTERVAL '30 days') as active_30d
            FROM users
        """)
        return dict(result)
```

### 1.2 Добавление обработчика "Мои результаты"
**Проблема:** Кнопка есть в меню, но обработчик отсутствует.

**Решение:**
```python
# В bot/handlers/stats.py добавить:

@router.callback_query(F.data == "my_results")
async def on_my_results(call: CallbackQuery, db: VKDatabase):
    """Показать историю запросов пользователя"""
    user_id = call.from_user.id
    
    # Получаем последние 10 результатов
    results = await db.get_user_recent_results(user_id, limit=10)
    
    if not results:
        await call.message.edit_text(
            "У вас пока нет сохраненных результатов",
            reply_markup=back_to_menu_kb()
        )
        return
    
    # Формируем текст с результатами
    text = "📊 <b>Ваши последние результаты:</b>\n\n"
    for i, result in enumerate(results, 1):
        text += f"{i}. {result['link']}\n"
        if result['phones']:
            text += f"   📱 {', '.join(result['phones'])}\n"
        if result['full_name']:
            text += f"   👤 {result['full_name']}\n"
        text += f"   📅 {result['checked_at'].strftime('%d.%m.%Y %H:%M')}\n\n"
    
    # Добавляем кнопки
    keyboard = InlineKeyboardMarkup(inline_keyboard=[
        [InlineKeyboardButton(text="📥 Скачать все", callback_data="export_history")],
        [InlineKeyboardButton(text="🏠 Главное меню", callback_data="main_menu")]
    ])
    
    await call.message.edit_text(text, reply_markup=keyboard)
```

### 1.3 Исправление обработчика статистики
**Проблема:** Вызывается несуществующий метод.

**Решение:**
```python
# В bot/handlers/stats.py исправить:

@router.callback_query(F.data == "user_stats")
async def on_user_stats(call: CallbackQuery, db: VKDatabase):
    """Статистика пользователя"""
    user_id = call.from_user.id
    stats = await db.get_user_statistics(user_id)
    
    text = f"""📈 <b>Ваша статистика:</b>

📊 Всего проверено: {stats['total_checked']}
✅ Найдено данных: {stats['found_data_count']}
📅 Дней активности: {stats['days_active']}
🎯 Эффективность: {(stats['found_data_count']/stats['total_checked']*100):.1f}%"""
    
    await call.message.edit_text(text, reply_markup=back_to_menu_kb())
```

---

## 💡 ЭТАП 2: Улучшения UX (3-4 дня)

### 2.1 Индикатор прогресса при загрузке файлов
**Реализация:**
- Добавить анимированный прогресс-бар при загрузке файла
- Показывать процент загрузки и оставшееся время
- Использовать `asyncio` для неблокирующих обновлений

```python
async def show_upload_progress(message: Message, current: int, total: int):
    """Обновление прогресса загрузки"""
    percent = (current / total) * 100
    bar = create_progress_bar(current, total)
    
    text = f"""📤 <b>Загрузка файла</b>
    
{bar}
Прогресс: {percent:.1f}%
Обработано строк: {current}/{total}"""
    
    await safe_edit_message(message, text)
```

### 2.2 Предварительный расчет стоимости
**Реализация:**
- Перед обработкой показывать количество новых ссылок
- Рассчитывать требуемое количество поисков
- Предупреждать если баланса недостаточно

```python
async def show_cost_estimation(links_count: int, cached_count: int, balance: int):
    """Показ предварительной стоимости"""
    new_searches = links_count - cached_count
    
    text = f"""💰 <b>Предварительный расчет:</b>
    
📊 Всего ссылок: {links_count}
💾 Из кеша: {cached_count}
🔍 Новых проверок: {new_searches}

💳 Ваш баланс: {balance} поисков
💸 Будет использовано: {new_searches} поисков
💰 Останется: {balance - new_searches} поисков"""
    
    if new_searches > balance:
        text += "\n\n⚠️ <b>Недостаточно поисков!</b>"
    
    return text
```

### 2.3 Экспорт истории пользователя
**Реализация:**
- Добавить метод экспорта всех результатов пользователя
- Формировать Excel файл с полной историей
- Возможность фильтрации по датам

```python
async def export_user_history(user_id: int, date_from=None, date_to=None):
    """Экспорт истории в Excel"""
    results = await db.get_user_all_results(user_id, date_from, date_to)
    
    df = pd.DataFrame(results)
    df.columns = ['VK ссылка', 'Телефоны', 'Имя', 'Дата рождения', 'Дата проверки']
    
    file_path = f"history_{user_id}_{datetime.now().strftime('%Y%m%d')}.xlsx"
    df.to_excel(file_path, index=False)
    
    return file_path
```

### 2.4 Пауза и возобновление обработки
**Реализация:**
- Сохранение состояния обработки в сессии
- Кнопка паузы во время обработки
- Возможность продолжить с места остановки

```python
# В сессии сохраняем:
session = {
    'processing_state': 'paused',
    'processed_count': 150,
    'total_count': 500,
    'remaining_links': [...],
    'results_so_far': {...}
}

# При возобновлении:
async def resume_processing(user_id: int):
    session = await get_user_session(user_id)
    remaining = session['remaining_links']
    # Продолжаем с remaining
```

---

## 🛠 ЭТАП 3: Административный функционал (5-7 дней)

### 3.1 Журнал действий администраторов
**Реализация:**
- Создать таблицу `admin_logs` в БД
- Логировать все админские команды
- Возможность просмотра логов

```sql
CREATE TABLE admin_logs (
    id SERIAL PRIMARY KEY,
    admin_id BIGINT,
    action TEXT,
    target_user_id BIGINT,
    details JSONB,
    timestamp TIMESTAMP DEFAULT NOW()
);
```

### 3.2 Управление пользователями
**Функции:**
- Блокировка/разблокировка пользователей
- Установка лимитов на количество запросов
- Просмотр активности пользователя

```python
# Новые команды:
/block_user <user_id> - блокировка
/unblock_user <user_id> - разблокировка  
/set_limit <user_id> <daily_limit> - установка лимита
/user_info <user_id> - информация о пользователе
```

### 3.3 Мониторинг и алерты
**Реализация:**
- Отслеживание ошибок и сбоев
- Уведомления админам в Telegram
- Дашборд со статистикой

```python
async def send_admin_alert(error_type: str, details: str):
    """Отправка алерта админам"""
    for admin_id in ADMIN_IDS:
        try:
            await bot.send_message(
                admin_id,
                f"🚨 <b>Алерт системы</b>\n\n"
                f"Тип: {error_type}\n"
                f"Детали: {details}\n"
                f"Время: {datetime.now()}"
            )
        except:
            pass
```

### 3.4 Автоматический бэкап БД
**Реализация:**
- Ежедневный бэкап в 3:00
- Хранение последних 7 бэкапов
- Возможность ручного бэкапа

```python
# Используем APScheduler
from apscheduler.schedulers.asyncio import AsyncIOScheduler

scheduler = AsyncIOScheduler()

@scheduler.scheduled_job('cron', hour=3)
async def auto_backup():
    """Автоматический бэкап БД"""
    backup_file = f"backup_{datetime.now().strftime('%Y%m%d')}.sql"
    
    await db.create_backup(backup_file)
    
    # Удаляем старые бэкапы
    await cleanup_old_backups(keep_days=7)
    
    # Уведомляем админов
    await notify_admins(f"✅ Бэкап создан: {backup_file}")
```

---

## 🚀 ЭТАП 4: Новые функции (7-10 дней)

### 4.1 Массовая проверка по ID
**Реализация:**
- Принимать список чисел (ID пользователей)
- Автоматически формировать ссылки вида vk.com/id{number}
- Обрабатывать как обычные ссылки

```python
def parse_ids_from_text(text: str) -> List[str]:
    """Извлечение ID и формирование ссылок"""
    ids = re.findall(r'\b\d{5,10}\b', text)
    links = [f"https://vk.com/id{id}" for id in ids]
    return links
```

### 4.2 История изменений профиля
**Реализация:**
- Сохранять версии данных при каждой проверке
- Отслеживать изменения (новые телефоны, смена имени)
- Уведомлять об изменениях

```sql
CREATE TABLE profile_history (
    id SERIAL PRIMARY KEY,
    link TEXT,
    phones JSONB,
    full_name TEXT,
    birth_date TEXT,
    changed_at TIMESTAMP DEFAULT NOW(),
    changes JSONB -- что именно изменилось
);
```

### 4.3 Реферальная система
**Реализация:**
- Генерация уникальных реферальных ссылок
- Начисление бонусов за приглашенных
- Статистика рефералов

```python
# Таблица рефералов
CREATE TABLE referrals (
    id SERIAL PRIMARY KEY,
    referrer_id BIGINT,
    referred_id BIGINT,
    bonus_given BOOLEAN DEFAULT FALSE,
    created_at TIMESTAMP DEFAULT NOW()
);

# Реферальная ссылка
https://t.me/bot_username?start=ref_{user_id}
```

### 4.4 Webhook для внешних сервисов
**Реализация:**
- REST API для проверки ссылок
- Авторизация по токену
- Лимиты на количество запросов

```python
from fastapi import FastAPI, HTTPException

app = FastAPI()

@app.post("/api/check_link")
async def check_link(link: str, token: str):
    """API endpoint для проверки ссылки"""
    if not validate_token(token):
        raise HTTPException(401, "Invalid token")
    
    result = await vk_service.search_vk_link(link)
    return result
```

---

## 📊 Оценка трудозатрат

| Этап | Описание | Срок | Приоритет |
|------|----------|------|-----------|
| **Этап 1** | Критические исправления | 1-2 дня | 🔴 Критический |
| **Этап 2** | Улучшения UX | 3-4 дня | 🟠 Высокий |
| **Этап 3** | Админ функционал | 5-7 дней | 🟡 Средний |
| **Этап 4** | Новые функции | 7-10 дней | 🟢 Низкий |
| **ИТОГО** | Полная доработка | 16-23 дня | - |

---

## 🎯 Ожидаемые результаты

### После Этапа 1:
- ✅ Все базовые функции работают корректно
- ✅ Нет критических ошибок
- ✅ Админские команды функционируют

### После Этапа 2:
- ✅ Улучшен пользовательский опыт
- ✅ Прозрачность процессов
- ✅ Возможность управления обработкой

### После Этапа 3:
- ✅ Полный контроль для администраторов
- ✅ Мониторинг и алерты
- ✅ Безопасность данных (бэкапы)

### После Этапа 4:
- ✅ Расширенная функциональность
- ✅ Возможности монетизации
- ✅ API для интеграций

---

## 🔧 Технический стек

- **Backend:** Python 3.10+, aiogram 3.x, asyncio
- **Database:** PostgreSQL 14+, asyncpg
- **Cache:** Redis (опционально)
- **API:** FastAPI (для webhook)
- **Scheduler:** APScheduler
- **Export:** pandas, openpyxl
- **Monitoring:** logging, Sentry (опционально)

---

## 📝 Порядок внедрения

1. **Тестирование на dev-окружении**
   - Создать копию БД для тестов
   - Проверить все сценарии использования

2. **Поэтапный деплой**
   - Сначала критические исправления
   - Мониторинг работы 24 часа
   - Затем следующий этап

3. **Документирование**
   - Обновить README
   - Создать CHANGELOG
   - Документировать API

4. **Обучение**
   - Инструкция для администраторов
   - FAQ для пользователей
   - Видео-туториалы (опционально)

---

## ⚠️ Риски и митигация

| Риск | Вероятность | Митигация |
|------|-------------|-----------|
| Потеря данных при миграции | Низкая | Обязательные бэкапы перед изменениями |
| Превышение лимитов VK бота | Средняя | Система очередей и распределения нагрузки |
| Блокировка Telegram бота | Низкая | Соблюдение лимитов API, антиспам |
| Недовольство пользователей | Средняя | Постепенное внедрение, обратная связь |

---

## 📞 Контакты для вопросов

При возникновении вопросов по реализации обращаться к:
- Техническому руководителю проекта
- DevOps инженеру (для инфраструктуры)
- DBA (для изменений в БД)

---

*Документ обновлен: 29.08.2025*