# VK Data Assistant Bot

Telegram бот для поиска контактных данных по ссылкам VK через специализированные боты.

## 🚀 Возможности

- 📎 Обработка Excel файлов со ссылками VK
- 🔗 Поиск данных по отдельным ссылкам  
- 📊 Формирование удобных отчетов в Excel
- 💾 Кеширование результатов в PostgreSQL
- 🔍 Поиск по номеру телефона в базе
- 📈 Статистика использования

## 📋 Требования

- Python 3.10+
- PostgreSQL
- Redis (опционально, но рекомендуется)
- Telegram аккаунт для работы с VK ботом

## 🛠 Установка

1. **Клонируйте репозиторий:**
```bash
git clone <repository-url>
cd vk-data-assistant
```

2. **Создайте виртуальное окружение:**
```bash
python -m venv .venv
source .venv/bin/activate  # Linux/Mac
# или
.venv\Scripts\activate  # Windows
```

3. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

4. **Настройте PostgreSQL:**
```sql
CREATE DATABASE vk_data;
CREATE USER postgres WITH PASSWORD 'your_password';
GRANT ALL PRIVILEGES ON DATABASE vk_data TO postgres;
```

5. **Настройте Redis (опционально):**
```bash
# Ubuntu/Debian
sudo apt install redis-server
sudo systemctl start redis

# Или используйте Docker
docker run -d -p 6379:6379 redis:alpine
```

## ⚙️ Конфигурация

1. **Создайте файл `.env` на основе примера:**
```bash
cp .env.example .env
```

2. **Заполните необходимые параметры:**
```env
# Telegram Bot Configuration
BOT_TOKEN=YOUR_BOT_TOKEN

# VK Bot Configuration
VK_BOT_USERNAME=eye_of_god_bot  # Имя бота для поиска (без @)

# Telethon Configuration
API_ID=YOUR_API_ID
API_HASH=YOUR_API_HASH
ACCOUNT_PHONE=+7XXXXXXXXXX

# PostgreSQL Configuration
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=vk_data
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_password

# Admin IDs (через запятую)
ADMIN_IDS=123456789,987654321
```

### Получение необходимых данных:

- **BOT_TOKEN**: Создайте бота через [@BotFather](https://t.me/BotFather)
- **API_ID и API_HASH**: Получите на [my.telegram.org](https://my.telegram.org)
- **VK_BOT_USERNAME**: Имя бота для поиска данных (например: `eye_of_god_bot`)

## 🚀 Запуск

### Обычный запуск:
```bash
python run.py
```

### Запуск через Docker:
```bash
docker-compose up -d
```

### Очистка проекта:
```bash
python cleanup_project.py
```

## 📖 Использование

### Для пользователей:

1. **Начало работы:**
   - Отправьте команду `/start`
   - Примите условия использования

2. **Поиск по ссылкам:**
   - Отправьте Excel файл со ссылками VK
   - Или отправьте ссылки прямо в сообщении

3. **Поиск по телефону:**
   - Используйте команду `/findphone`
   - Отправьте номер телефона

### Для администраторов:

- `/botstatus` - Статус VK бота
- `/dbstats` - Статистика базы данных
- `/broadcast` - Рассылка сообщений
- `/debug` - Отладочная информация

## 🏗 Структура проекта

```
vk-data-assistant/
├── bot/                    # Основной модуль бота
│   ├── handlers/          # Обработчики команд
│   ├── keyboards/         # Клавиатуры
│   ├── middleware/        # Middleware
│   └── utils/            # Утилиты
├── services/             # Внешние сервисы
│   ├── vk_service.py    # Работа с VK ботом
│   └── excel_service.py # Обработка Excel
├── db_module/           # Работа с БД
├── data/               # Временные файлы
├── logs/              # Логи
└── run.py            # Точка входа
```

## ⚠️ Важные замечания

1. **Лимиты VK бота:**
   - У каждого бота есть ограничения на количество запросов
   - При достижении лимита бот уведомит вас

2. **Безопасность:**
   - Не передавайте файл `.env` третьим лицам
   - Используйте бота только в законных целях

3. **База данных:**
   - Результаты кешируются для ускорения повторных запросов
   - Регулярно делайте бэкапы БД

## 🐛 Решение проблем

### Ошибка "Nobody is using this username":
- Проверьте правильность `VK_BOT_USERNAME` в `.env`
- Убедитесь, что бот существует и доступен

### Ошибка подключения к PostgreSQL:
- Проверьте, запущен ли PostgreSQL
- Проверьте правильность данных в `.env`

### Не приходят результаты:
- Проверьте баланс VK бота командой `/botstatus`
- Убедитесь, что ссылки корректны

## 📝 Лицензия

Этот проект предназначен только для законного использования. Разработчики не несут ответственности за неправомерное использование.

## 🤝 Поддержка

При возникновении проблем создайте Issue в репозитории или обратитесь к администраторам.