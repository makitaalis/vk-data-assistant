Интервалы между поисками:

- В `bot/config.py` увеличены и параметризованы задержки между запросами: `INTER_REQUEST_DELAY_MIN/MAX` и `EXTRA_INTER_REQUEST_DELAY_MIN/MAX` теперь по умолчанию 3.5–7.0 секунды, смещение `EXTRA_INTER_REQUEST_DELAY_BIAS` обнулено. Таким образом паузы между поисковыми запросами выбираются случайно в диапазоне 3.5–7 секунд.
