# План доработки: параллельные Telegram-сессии

## 1. Цели
- Поддержать одновременную работу через две независимые Telegram-сессии.
- Дать возможность админам выбирать активные сессии (primary / secondary / обе).
- Распараллелить обработку загруженных файлов и повысить отказоустойчивость.

## 2. Этапы работ

### Этап A. Конфигурация и структура
1. `.env`  
   - Добавить блок `# Telegram Sessions` с переменными `SESSION_NAME_PRIMARY`, `ACCOUNT_PHONE_PRIMARY`, `SESSION_NAME_SECONDARY`, `ACCOUNT_PHONE_SECONDARY`, `ENABLE_PRIMARY`, `ENABLE_SECONDARY`, `SESSION_MODE`.  
   - Сохранить обратную совместимость: если указаны только старые `SESSION_NAME/ACCOUNT_PHONE`, автоматически заполнять primary.
2. `bot/config.py`  
   - Создать dataclass `TelegramSessionConfig` (name, phone, enabled, storage_dir).  
   - Функция `load_sessions()` возвращает список активных сессий и текущий режим (`SESSION_MODE`).  
   - Вынести пути `SESSION_DIR / session_name` в отдельное свойство.  
3. Структура файлов  
   - Скрипт миграции: переместить `data/sessions/user_session_<id>*.session` в `data/sessions/<session_name>/`.  
   - Обновить код, который читает StringSession (`services/vk_multibot_service.py`, `bot/main.py`), чтобы использовать новые пути.

### Этап B. Мульти-сессия в VKMultiBotService
1. Интерфейс конструктора  
   - Принимать список `TelegramSessionConfig` вместо одного `session_base_name`.  
   - Каждой сессии сопоставлять собственный список `BotState`; хранить в структуре `{session_name: [BotState...]}`.
2. Инициализация  
   - Для каждой активной сессии создавать `session_dir`, загружать/конвертировать `.session_string`.  
   - Вести флаги `is_enabled`, `hold_until`, `last_error` на уровне сессии.
3. Планировщик  
   - Реализовать round-robin по сессиям (пропуская отключённые/на hold).  
   - Внутри выбранной сессии использовать текущую реализацию выбора VK-бота.  
   - Если `SESSION_MODE=both`, поддерживать дублирование запросов: одна ссылка → две независимые отправки и агрегация результатов.
4. Динамическое управление  
   - Предусмотреть методы `enable_session(name)`, `disable_session(name)` и обновление конфигурации в рантайме (через Redis/ConfigService).  
   - При выключении сессии корректно отключать клиентов Telethon, при включении — повторно инициализировать.

### Этап C. Расширение админ-панели
1. Команды  
   - `/session_status` — список сессий, их состояние (online/offline/hold), активный режим, количество доступных VK-ботов.  
   - `/session_enable <name>`, `/session_disable <name>` — обновляют Redis и вызывают методы сервиса.
2. UI  
   - Inline-клавиатура с кнопками `🟢 Primary`, `🟠 Secondary`, `🟣 Both` для переключения режима (`SESSION_MODE`).  
   - Кнопки для быстрого включения/выключения конкретных сессий.  
   - Сообщения для админов о результате операции (успех / ошибка).
3. Сервис конфигурации  
   - Добавить ключи Redis `bot:session:mode` и `bot:session:enabled:<name>`.  
   - `ConfigService` должен уметь читать/изменять эти значения и оповещать `VKMultiBotService`.

### Этап D. Параллельная обработка
1. Очередь заданий  
   - В `bot/handlers/files.py` и связанных сервисах хранить состояния загрузки таким образом, чтобы для каждой ссылки была информация о назначенной сессии (или списке сессий).  
   - Для режима “both” — дубль задания в обе очереди с общим идентификатором.
2. Исполнение  
   - Реализовать асинхронный диспетчер, который выдаёт следующую ссылку подходящей сессии (учитывая лимиты/hold).  
   - Для режима “split” делить входной список поровну (или по весам) между активными сессиями.
3. Агрегация  
   - Ввести структуру `ProcessingResult` с полями `session_name`, `link`, `status`, `data`.  
   - При получении результата обновлять объединённый список ответов, избегая дубликатов телефонов/профилей.

### Этап E. Мониторинг и диагностика
1. Команды  
   - `/botstatus` → выводить статус по каждой сессии (авторизован ли Telethon клиент, сколько ботов доступно, кол-во ошибок).  
   - `/balance` → собирать баланс по каждой сессии отдельно (если боты имеют разные кошельки).
2. Логи  
   - В `services/logger_service.py` добавить контекст `session=<name>` для сообщений Telethon.  
   - В `logs/bot_restart.log` и ошибках явно указывать, какая сессия вызвала исключение.
3. Оповещения  
   - Админ-уведомления (бот offline, лимит, flood) должны включать имя сессии и список затронутых VK-ботов.

### Этап F. Миграция и тесты
1. Авторизация  
   - Скрипт `auth_session_secondary.py` (копия основного со вторым `SESSION_NAME`) + инструкция в README.  
   - Автоматизированная проверка `python check_session.py --session secondary`.
2. Тестирование  
   - `test_multi_session.py`: мок `VKMultiBotService` с двумя сессиями, генерация ссылок, проверка распределения, тайм-аутов и агрегации.  
   - Расширение существующих `test_cache_*` для проверки, что кеш учитывает идентификатор сессии (если требуется).
3. Документация  
   - Обновить `README.md`, `COMMANDS_SUMMARY.md`, `FULL_PROJECT_INFO.md` с описанием новых команд и переменных.  
   - В `MULTI_SESSION_PLAN.md` фиксировать прогресс и фактические изменения.

## 3. Прогресс
| Время (UTC)           | Этап | Статус | Комментарий |
|-----------------------|------|--------|-------------|
| 2025-11-26 17:41:31   | A-F  | План   | Сформирован базовый план работ и структура отчётности. |
| 2025-11-26 17:43:04   | A-F  | Актуализация | План углублён: добавлены конкретные файлы, структуры данных и административные шаги. |
| 2025-11-26 17:48:27   | A    | В работе | Добавлены новые переменные конфигурации, структура `TelegramSessionConfig` и автоматическая миграция файлов сессий в индивидуальные каталоги. |
| 2025-11-26 17:58:05   | B    | В работе | Переписан `VKMultiBotService`: поддержка нескольких Telegram-сессий, выбор режима (`primary/secondary/both`), методы включения/отключения сессий и ротация ботов по сессиям. |
| 2025-11-26 18:46:41   | C    | В работе | Добавлены команды `/session_status`, `/session_enable`, `/session_disable`, интерактивное меню управления сессиями и интеграция с ConfigService/Redis; `botstatus` расширен сессиями. |
| 2025-11-26 18:59:36   | D    | В работе | Обновлен `start_processing` в `bot/handlers/search.py`: ссылки распределяются между Telegram-сессиями, добавлены воркеры вместо `process_queue`, подготовлена основа для параллельной обработки. |
| 2025-11-26 19:02:39   | D    | В работе | Реализовано детерминированное распределение ссылок по активным сессиям (round-robin), каждая ссылка закрепляется за конкретной сессией без дубликатов. |
| 2025-11-26 19:04:55   | D    | В работе | Добавлена фиксация назначений `link → session` в сессии пользователя и проставление `session_name` в результатах для последующего мониторинга/возобновления обработки. |
| 2025-11-26 19:08:42   | E    | В работе | `VKMultiBotService` теперь проставляет `session_name`/`bot_username` во всех ответах и отчетах баланса, что готовит основу для мониторинга и логов по сессиям. |
| 2025-11-26 19:10:39   | E    | В работе | `/botstatus` и меню управления сессиями показывают запросы/ошибки и статусы hold/limit/removed для каждой Telegram-сессии (новые метрики из `get_stats`). |
| 2025-11-26 19:13:47   | D    | В работе | Прогресс-бар теперь показывает распределение ссылок по сессиям (assignments + per-session прогресс), данные сохраняются в пользовательской сессии. |
| 2025-11-26 19:15:15   | C    | В работе | Кнопки меню управления сессиями отображают доступность, запросы/ошибки и статусы hold/limit/removed для быстрой диагностики. |
| 2025-11-26 19:17:18   | D    | В работе | Итоговые сообщения и лимит-нотификации включают сводку по сессиям (из сохранённых assignments/progress), улучшая прозрачность для пользователей. |
| 2025-11-26 19:18:59   | E    | Актуализация | Мониторинг и интерфейсы админов полностью отражают состояние всех сессий: метаданные в ответах, статистика в `/botstatus`, клавиатура с показателями нагрузки. |
| 2025-11-26 19:30:50   | C-D  | В работе | Добавлены слоты Slot A/Slot B: ConfigService + VKMultiBotService управляют активными сессиями, админ-меню позволяет назначать/очищать и мгновенно переключать рабочие аккаунты. |
| 2025-11-26 19:38:44   | C    | В работе | Реализован мастер `/session_auth`: интерактивная авторизация через бот, запись session_string, автоматическое назначение слота и обновление VKMultiBotService. |
| 2025-11-26 20:08:49   | C    | В работе | Команды больше не перехватываются search-хендлером: `/session_status` и `/session_auth` работают через новое меню, текстовые ответы идут по мастеру авторизации без сбоев. |
| 2025-11-26 20:13:46   | C    | В работе | Добавлена secondary-сессия `user_session_2` в конфиг и слот B, бот перезапущен; `/session_auth` принимает имя, обе сессии видны в меню. |
| 2025-11-26 20:19:22   | C    | В работе | Хендлер `/session_auth` больше не блокирует другие команды (используем SkipHandler), `/session_status` снова отвечает. |
| 2025-11-26 20:31:38   | D    | В работе | Исправлена сериализация `duplicate_check` и баланс-чек: Redis больше не падает на списках, перед запуском проверяется `vk_service`. |
| 2025-11-26 20:46:00   | B-D  | В работе | Добавлен контроль задержек: каждая Telegram-сессия выдерживает паузу 1.0–1.5 c между запросами (slot-aware throttle). |
| 2025-11-26 20:55:12   | D    | В работе | Распределение ссылок фиксировано по слотам (первая половина Slot A, вторая Slot B), чтобы исключить пересечения и упростить аудит. |
| 2025-11-26 19:18:11   | F    | В работе | Создан скрипт `auth_session_secondary.py` для авторизации второй Telegram-сессии; подготовка к тестированию Stage F. |
